# Setup Instructions
1. Open terminal in this folder.
2. Run `python -m venv .venv`
3. Run `.\.venv\Scripts\activate` (Windows)
4. Run `pip install -r requirements.txt`
5. Run tests: `python -m pytest tests/test_integrity.py`